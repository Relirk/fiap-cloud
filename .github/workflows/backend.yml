# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [master]

jobs:
  deploy-beta-job:
    name: Deploy FIAP Cloud API
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          ref: master
          node-version: 12

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.FIAP_SSH_KEY }}
          name: id_rsa # optional
          known_hosts: ${{ secrets.FIAP_KNOWN_HOSTS }}
          # config: ${{ secrets.CONFIG }} # ssh_config; optional

      # # ********************* Setup AWS
      # - name: Launch AWS EC2
      #   id: aws-credentials
      #   if: success()
      #   run: |
      #     ls -lah
      #     curl https://firebasestorage.googleapis.com/v0/b/portifoil-bcd7d.appspot.com/o/fiap.pem\?alt\=media\&token\=ee0a94df-5670-4ca9-b6a9-4111b3ec8be1-s --output key.pem
      #     chmod 777 key.pem
      #     ls -lah
      #     ssh -T -i "key.pem" ubuntu@ec2-54-157-139-9.compute-1.amazonaws.com

      # ********************* Install
      - name: Installing dependencies
        id: install
        if: success()
        # if: steps.npm_cache.outputs.cache-hit != 'true'
        run: |
          ls
      # ********************* Build
      # - name: Building structure
      #   id: build
      #   if: success()
      #   run: |
      #     rm -rf dist
      #     npm run build
      # ********************* Deploy Prod
      # - name: Deploy
      #   if: success()
      #   run: |
      #     cd tools
      #     ./backend_deploy.sh
      #     echo "commit $(git rev-parse HEAD) published successfully"
